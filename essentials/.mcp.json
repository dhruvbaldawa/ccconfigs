{
  "mcpServers": {
    "context7": {
      "type": "sse",
      "url": "${MCP_PROXY_HOST}/context7/sse",
      "headers": {
        "Authorization": "Basic ${MCP_PROXY_AUTH}"
      }
    },
    "parallel-search": {
      "type": "sse",
      "url": "${MCP_PROXY_HOST}/parallel-search/sse",
      "headers": {
        "Authorization": "Basic ${MCP_PROXY_AUTH}"
      }
    },
    "parallel-research": {
      "type": "sse",
      "url": "${MCP_PROXY_HOST}/parallel-research/sse",
      "headers": {
        "Authorization": "Basic ${MCP_PROXY_AUTH}"
      }
    }
  }
}
